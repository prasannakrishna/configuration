spring:
  application:
    name: communityManager
  security:
    user:
      name: admin
      password: admin123
  datasource:
    url: 'jdbc:postgresql://localhost:5432/communitydb'
    username: prasannakrishnabhagwat
    password: admin123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  data:
    elasticsearch:
      cluster-name: docker-cluster
      cluster-nodes: 'localhost:9300'
server:
  port: 8081
  error:
    include-message: always
    include-stacktrace: never
logging:
  level:
    org:
      springframework:
        security: DEBUG
